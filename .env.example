# Cobo API credentials (never hard-code these in source!)
COBO_API_KEY=your_api_key_here         # Public API Key (from Cobo Portal)
COBO_API_SECRET=your_api_secret_here   # Private API Secret (hex-encoded private key)
COBO_BASE_URL=https://api.cobo.com     # Base URL for Cobo API (e.g., production endpoint)
